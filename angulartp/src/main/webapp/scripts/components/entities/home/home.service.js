'use strict';

angular.module('angulartpApp')
    .factory('Home', function ($resource) {
        return $resource('api/homes/:id', {}, {
            'query': { method: 'GET', isArray: true},
            'get': {
                method: 'GET',
                transformResponse: function (data) {
                    data = angular.fromJson(data);
                    return data;
                }
            },
            'update': { method:'PUT' }
        });
    });
